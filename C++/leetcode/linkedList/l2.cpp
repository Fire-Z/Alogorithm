/*
No.23 level.hard Name.合并K个有序列表
*/

#include <iostream>
#include <vector>
#include "structs.h"
using namespace std;

class Solution {
public:
    ListNode* mergeKLists(vector<ListNode*>& lists) {
        // 标记每个链表的现处位置
        vector<int> locators(lists.size(), 0);
        ListNode* rtn;



        return rtn;
        
    }
};

int main()
{
    
}