/*
No. level.easy Name.
*/

#include <iostream>
#include <vector>
#include <map>
using namespace std;

class Solution {
public:
    bool divideArray(vector<int>& nums) {
        map<int, int> numCount;
        for (int i : nums) {
            if (numCount.find(i) != map<int, int>::)
        }
    }
};

int main()
{
    cout << "test" << endl;
}